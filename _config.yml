# Site settings
title: David Wong
description: Building intelligent apps with AI tools
author: David Wong
url: "https://davidwyh-dev.github.io"
baseurl: "" # Leave empty for user site (username.github.io)

# Build settings
markdown: kramdown
highlighter: rouge

# Exclude files from build
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor

# Collections and defaults
defaults:
  - scope:
      path: ""
    values:
      layout: default
